<%= form_with(model: loan, local: true) do |form| %>
  <% if loan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(loan.errors.count, "error") %> prohibited this loan from being saved:</h2>

      <ul>
      <% loan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :loan_name %>
  </div>

  <div class="field">
    <%= form.label :book %>
    <%= form.text_field :book, id: :loan_book %>
  </div>

  <div class="field">
    <%= form.label :days %>
    <%= form.select :days, (1..30), id: :loan_book %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
